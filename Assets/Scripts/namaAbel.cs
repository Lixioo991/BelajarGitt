using UnityEngine;

public class namaAbel : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello, Abel!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
